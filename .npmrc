# Keep exact sibling versions in package.json so npm pack stays publishable.
# Link matching workspace packages instead of downloading them from the registry.
link-workspace-packages=true
save-workspace-protocol=false
